<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd//mybatis-3-mapper.dtd">
<mapper namespace="com.zkl.l_music.dao.AlbumDao">

    <resultMap id="albumMap" type="AlbumEntity" >
        <id column="id" property="id"/>
        <result column="name" property="name" />
        <result column="time" property="time" />
        <result column="hot" property="hot" />
        <association property="singerId" column="singer_id"
                     javaType="com.zkl.l_music.dao.SingerDao.selectById"/>
    </resultMap>

    <select id="selectAlbumsBySinger" resultMap="albumMap">
        SELECT id,name,time,hot,singer_id FROM album
        WHERE singer_id = #{singerId} ORDER BY time desc;
    </select>

    <select id="selectNewAlbums" resultMap="albumMap">
        SELECT TOP 5 id,name,time,hot,singer_id FROM album
        ORDER BY time desc;
    </select>

</mapper>