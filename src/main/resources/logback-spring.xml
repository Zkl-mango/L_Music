<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <property name="logPath" value="/tmp/lmusicLog/log" />
    <!--输出到控制台-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %contextName [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--输出到文件-->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logPath}/miniprogram%d{yyyy-MM-dd}</fileNamePattern>
            <maxHistory>20</maxHistory>        <!--日志最大保存时间（天）-->
            <totalSizeCap>1GB</totalSizeCap>   <!--单个日志最大容量-->
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %contextName [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--root是必写的节点，用来指定日志输出级别-->
    <root level="info">
        <appender-ref ref="console" />
        <appender-ref ref="file" />
    </root>

    <root level="error">
        <appender-ref ref="console" />
        <appender-ref ref="file" />
    </root>

    <!--打印info级别的日志-->
    <logger name="com.zkl.l_music.controller" level="info" additivity="false">
        <appender-ref ref="console"/>
    </logger>

    <logger name="com.zkl.l_music.controller" level="info">
        <appender-ref ref="file"/>
    </logger>

</configuration>
